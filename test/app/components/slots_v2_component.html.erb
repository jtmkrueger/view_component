<div class="card <%= @class_names %>">
  <div class="title">
    <%= title %>
  </div>
  <div class="subtitle">
    <%= subtitle %>
  </div>
  <div class="nav">
    <%= tabs.inspect %>
    <% if tabs.any? %>
      <% tabs.each do |tab| %>
        <div class="tab">
          <%= tab %>
        </div>
      <% end %>
    <% else %>
      No tabs provided
    <% end %>
  </div>
  <div class="body">
    <% if items.any? %>
      <% items.each do |item| %>
        <div class="item <%= item.class_names %>">
          <%= item %>
        </div>
      <% end %>
    <% else %>
      No items provided
    <% end %>
  </div>

  <%= footer %>
</div>
